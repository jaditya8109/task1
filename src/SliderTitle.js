import React from 'react';

function SliderTitle(props) {
    return (
        <div className='title'>
            <h3><span>~</span>{props.sliderTitle}</h3>
        </div>
    );
}

export default SliderTitle;